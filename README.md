# Trained Models Directory
# The .pkl files (knn_model.pkl, demand_model.pkl, waste_model.pkl) are generated
# automatically here upon running `python train_all.py` or upon the first launch of `python app.py`.
